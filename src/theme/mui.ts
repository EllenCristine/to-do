import { createTheme } from "@mui/system";

const theme = createTheme({
    palette: {
        primary: {
            main: '#CED3DC',
            light: '#FCF7F8'
        },
        secondary: {
            main: '#4E8098',
            light: '#90C2E7',
        },
        red: {
            main: '#A31621',
        },
    }
})

export default theme;